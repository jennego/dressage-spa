import CreateTest from "./createForm";

const Admin = () => {
  return (
    <div>
      <h1>Admin</h1>
      <p> Create a test (that exists, that is)</p>
      <CreateTest />
    </div>
  );
};
export default Admin;
